## [55. 跳跃游戏](https://leetcode.cn/problems/jump-game/) 

![image-20250526195032500](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250526195032500.png) 

```go
//可以设置可到的最远的范围，通过比较当前索引和范围，看能否达到
func canJump(nums []int) bool {
    maxlongth:=0 
    for i,jump :=range nums {// 遍历数组，i 是当前位置，jump 是当前能跳的最大步数
        if i>maxlongth {
            // 如果当前位置 i 已经超过了能到达的最远位置
            return false             // 说明无法到达当前位置，直接返回 false
        }
        maxlongth=max(maxlongth,i+jump)
    }
    return true
}
```

