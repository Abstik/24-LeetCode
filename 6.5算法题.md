## [1. 两数之和](https://leetcode.cn/problems/two-sum/) 

![image-20250604163309875](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250604163309875.png) 

```go
//运用哈希表，将每个数的索引存储在哈希表中，遍历数组的同时检查能达到目标和的数是否存储在哈希表中
func twoSum(nums []int, target int) []int {
    idx:=make(map[int]int)
    for i,v :=range nums {
        if j, ok :=idx[target-v]; ok {
            /*j代表idx[target-v]的值，即target-v的索引，ok是bool值，如果target-v这个数在哈希表中存在，为true;
            ok是if ok==true的简写，如果为true,返回相应坐标*/
            return []int {i,j}
        }
        idx[v]=i
    }
    return nil
}
```

