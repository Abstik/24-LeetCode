## [968. 监控二叉树 - 力扣（LeetCode）](https://leetcode.cn/problems/binary-tree-cameras/description/)

![image-20250610224428904](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202506102244333.png)

```go
var result int
func dfs(root *TreeNode) int {
    if (root == nil) {
        return 2
    }
    var left int = dfs(root.Left)
    var right int = dfs(root.Right)
    // 若左右孩子均已经覆盖，无需加摄像头，该节点无覆盖
    if left == 2 && right == 2 {
        return 0
    }
    // 左右孩子有任意一个无覆盖，需要加摄像头，该节点有摄像头
    if left == 0 || right == 0 {
        result++;
        return 1
    }
    // 左右孩子有任意一个有摄像头，无需摄像头，该节点已覆盖
    if left == 1 || right == 1 {
        return 2
    }

    return -1 
    
}

func minCameraCover(root *TreeNode) int {
    result = 0
    if dfs(root) == 0 {
        // root无覆盖
        result++
    }
    return result
}
```

