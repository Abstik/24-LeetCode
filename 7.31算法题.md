## [134. 加油站](https://leetcode.cn/problems/gas-station/) 

![image-20250529215110253](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250529215110253.png) 

```go
//贪心策略：如果总油量足够，那么一定存在一个起点，使得从该点出发可以绕行一周。这个起点就是油量最低点的下一个加油站。
//汽车从 start = 0 出发，油量 s 会不断变化，最低点出现在 i = k（即 minS 对应的位置）。
//此时 s 最小，说明从 0 到 k 的油量消耗最大，剩余油量最少。
//如果从 k+1 出发，相当于绕过了最“耗油”的路段，使得后续油量始终 ≥ 0。
//不会计算出ans=n,如果最后一轮循环 s<minS，那么 s 必然小于 0，这会导致最终返回 −1。
func canCompleteCircuit(gas []int, cost []int) int {
    ans,mingas,gasadd:=0,0,0
    for i,v :=range gas {
        gasadd+=v-cost[i]
        if gasadd<mingas {
            mingas=gasadd
            ans=i+1
        }
    }
    if gasadd<0 {
        return -1
    }
    return ans
}
```

