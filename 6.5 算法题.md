## [202. 快乐数](https://leetcode.cn/problems/happy-number/)

![image-20250605203241679](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250605203241679.png)



~~~go
func isHappy(n int) bool {
	// map记录出现过的数字
	m := make(map[int]bool)
	// n不为1 / 还未进入循环
	for n != 1 && !m[n] {
		n, m[n] = cal(n), true // 更新并记录已出现的数避免循环
	}
	return n == 1
}

func cal(n int) int {
	sum := 0
	for n > 0 {
		sum += int(math.Pow(float64(n%10), 2))
		n = n / 10
	}
	return sum
}
~~~



## [1. 两数之和（使用哈希版）](https://leetcode.cn/problems/two-sum/)

![image-20250605204521446](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250605204521446.png)



~~~go
func twoSum(nums []int, target int) []int {
	m := make(map[int]int)

	for i, v := range nums {
		num := target - v            // 需查找的差值
		if index, ok := m[num]; ok { // 存在差值
			return []int{index, i}
		}
		m[v] = i // 不存在，则将值和下标存入哈希表
	}
	// 使得能用O（n）复杂度完成查询
	return nil
}
~~~

