## [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)

![image-20250524200248505](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250524200248505.png)  

```go
func minSubArrayLen(target int, nums []int) int {
    //运用滑动窗口的思想
    n :=len(nums) //获取数组长度 n
    ans,left,sum :=n+1,0,0//ans 初始为 n+1（一个不可能的大值，用于后续比较）
//sum 初始为 0，用于记录当前窗口的和
//left 初始为 0，表示滑动窗口的左边界
    for right,x :=range nums {
        //使用 for-range 循环遍历数组，right 是当前右边界索引，x 是当前元素
//将当前元素 x 加到 sum 中，扩展窗口的右边界
        sum+=x
        for sum-nums[left]>=target {
            //内层 for 循环尝试缩小窗口：如果减去左边界元素后和仍大于等于 target，则移动左边界，减去左边界的值并向右移动左边界指针
            sum-=nums[left] 
            left++ 
        }
        if sum>=target {
            ans=min(ans,right-left+1)//检查当前窗口和是否满足 target
//如果满足，更新最小长度 ans 为当前窗口长度 (right-left+1) 和原 ans 中的较小值
        }
    }
    if ans<=n {
        //循环结束后，如果 ans 被更新过（即 ans <= n），返回最小长度
//否则返回 0，表示没有满足条件的子数组
        return ans
    }
    return 0
}
```

