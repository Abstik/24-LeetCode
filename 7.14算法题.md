## [56. 合并区间](https://leetcode.cn/problems/merge-intervals/) 

![image-20250604163338759](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250604163338759.png)

```go
//这里采用左端点升序排序，通过比较左端点是否小于等于右端点来看是否是重叠的
func merge(intervals [][]int) (ans [][]int) {
    sort.Slice(intervals,func(i,j int) bool {//左端点升序排序
        return intervals[i][0]<intervals[j][0]
    })
    for _,v :=range intervals {
        m:=len(ans)//用m来获取数组长度，也可以不断更新长度
        if m>0 && v[0]<=ans[m-1][1] {//如果数组里有值了，才能进行区间判断，如果遍历到的区间的左端点小于等于数组里当前区间的右端点，则为重叠区间
            ans[m-1][1]=max(v[1],ans[m-1][1])//更新右端点的最大值
        }else {
            ans=append(ans,v)//不满足条件直接加入
        }
    }
    return ans
}
```

