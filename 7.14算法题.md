## [106. 从中序与后序遍历序列构造二叉树](https://leetcode.cn/problems/construct-binary-tree-from-inorder-and-postorder-traversal/)

![image-20250524200222269](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250524200222269.png)

```go
//运用递归的思想，先找到根节点值，再锁定根节点值在数组中的位置，获取左边界以便获取左右子树，再递归左右子树创建二叉树 
func buildTree(inorder []int, postorder []int) *TreeNode {
    n:=len(postorder)//获取数组的长度，当长度为0时，说明该节点值为空
    if n==0 {
        return nil
    }
    leftSize :=slices.Index(inorder,postorder[n-1])//使用slices.Index函数找到根节点值在切片中的具体位置并设置为左边界，后序遍历数组的最后一个节点就是根节点值
    left:=buildTree(inorder[:leftSize],postorder[:leftSize])//这里要注意go语言中切片再切片时[a:b]，a是包含的而b是不包含的，所以这里的具体索引表示从0到leftSize减一，在中序数组和后序数组正好表示左子树的范围
    Right:=buildTree(inorder[leftSize+1:],postorder[leftSize:n-1])//n-1表示后序遍历数组去掉最后一个根节点
    return &TreeNode{postorder[n-1],left,Right}
   /* 值取后序遍历最后一个元素postorder[n-1]
左子树指向递归构建的left
右子树指向递归构建的right*/
    /*列举部分例子帮助理解
    &TreeNode{
    Val:   20,
    Left:  &TreeNode{15, nil, nil}, 
    Right: &TreeNode{7, nil, nil},  
    
    &TreeNode{
    Val:   3,
    Left:  &TreeNode{9, nil, nil},                   
    Right: &TreeNode{20, &TreeNode{15, nil, nil}, &TreeNode{7, nil, nil}}
}
}*/
}
```

