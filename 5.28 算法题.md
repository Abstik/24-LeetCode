## [93. 复原 IP 地址](https://leetcode.cn/problems/restore-ip-addresses/)

![image-20250528164515065](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250528164515065.png)



```go
var (
	path []string
	rt   []string
)

func restoreIpAddresses(s string) []string {
	path, rt = make([]string, 0, len(s)), make([]string, 0)
	var dfs func(string, int)
	dfs = func(s string, start int) {
		if len(path) == 4 { // 够四段后就不再继续往下递归
			if start == len(s) {
				str := strings.Join(path, ".")
				rt = append(rt, str)
			}
			return
		}
		for i := start; i < len(s); i++ {
			if i != start && s[start] == '0' { // 含有前导 0，无效
				break
			}
			str := s[start : i+1]
			num, _ := strconv.Atoi(str)
			if num >= 0 && num <= 255 {
				path = append(path, str) // 符合条件的就进入下一层
				dfs(s, i+1)
				path = path[:len(path)-1]
			} else { // 如果不满足条件，再往后也不可能满足条件，直接退出
				break
			}
		}
	}
	dfs(s, 0)
	return rt
}
```



## [78. 子集](https://leetcode.cn/problems/subsets/)

![image-20250528165207208](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250528165207208.png)



```go
var (
	path []int
	rt   [][]int
)

func subsets(nums []int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, len(nums))
	var dfs func([]int, int)
	dfs = func(nums []int, start int) {
		tmp := make([]int, len(path))
		copy(tmp, path) // 复制，避免后续修改影响结果
		rt = append(rt, tmp)

		for i := start; i < len(nums); i++ {
			path = append(path, nums[i])
			// 递归下一层，确保不重复
			dfs(nums, i+1)
			path = path[:len(path)-1] // 回溯移除最后添加的元素
		}
	}
	dfs(nums, 0)
	return rt
}
```

