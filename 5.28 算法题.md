## [738. 单调递增的数字 ](https://leetcode.cn/problems/monotone-increasing-digits/submissions/633117588/)

![image-20250528220312476](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202505282203653.png)

```go
//从最低位开始，如果高位数字大于低位数字，高位数字减一，后面的都变成9

func monotoneIncreasingDigits(n int) int {
    num := []byte(strconv.Itoa(n))   // strconv.Itoa(n)是把n转换成字符串，从而转换成byte，num是byte

    len := len(num)   //数组长度，从后往前遍历

    if len <= 1{    //一位 直接返回
        return n
    }

    for i:=len-1; i>0; i--{
        if num[i-1] > num[i]{
            num[i-1] -- 
            for j:=i; j<len; j++{   //后面的都赋值成9
                num[j] = '9'
            }
        }
    } 

    ret, _ := strconv.Atoi(string(num))  //num,err := strconv.Atoi(s)是把s转换成整数，err==nil转换成功，！=nil转换失败
    return ret
}
```

