## [59. 螺旋矩阵 II](https://leetcode.cn/problems/spiral-matrix-ii/) 

![image-20250525195430110](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250525195430110.png) 

```go
// 可以定义一个数a，初始化为一，所以外层循环可设置为a<=n*n，这样一直循环添加数即可，定义一个数组表示方向，定义一个方向指针表示此时是什么方向，在添加数之后判断方向是否需要改变
func generateMatrix(n int) [][]int {
    ans :=make([][]int,n)//切片的初始化，因为二维的也需要用到，所以还要用循环初始化二维数组的大小，此时已经知道要定义的切片大小，直接初始化为n即可
    for i :=range ans {
        ans[i]=make([]int,n)
    }
    a,x,y :=1,0,0 //初始化值，横竖坐标
    direction :=[][]int{{0,1},{1,0},{0,-1},{-1,0}}//初始化方向数组，分别表示右下左上
    dirIndex :=0//方向指针，用于改变方向
    for a<=n*n {
        ans[x][y]=a
        a++
        nextX :=x+direction[dirIndex][0]//分别获取下一次的行坐标和列坐标，用于之后判断是否需要改变方向
        nextY :=y+direction[dirIndex][1]
        if nextX<0 || nextX>=n ||nextY<0 || nextY>=n || ans[nextX][nextY]!=0 {
            //判断是否需要改变方向
            dirIndex=(dirIndex+1)%4 // 由于方向只有 4 种（0→1→2→3），当 dirIndex + 1 超过 3 时，% 4 会让它回到 0，实现 循环切换：
            nextX=x+direction[dirIndex][0]//获取下一次的左标
            nextY=y+direction[dirIndex][1]
        }
        x,y =nextX,nextY//赋值用于下一次添加数
    }
    return ans 
}
```

