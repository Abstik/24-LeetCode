## [1005. K 次取反后最大化的数组和 - 力扣（LeetCode）](https://leetcode.cn/problems/maximize-sum-of-array-after-k-negations/)

![image-20250715191739334](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202507151917433.png)

```go
//看能不能把最小的负数都变成正数,如果都变成了正数还有剩余的k,那再对最小的正数取反

func largestSumAfterKNegations(nums []int, k int) int {
    //排序
    sort.Ints(nums)
    //1.优先对绝对值最大的负数进行取反
    l := len(nums) //数组的长度
    for i:=0; i<l&&k>0; i++{
        if nums[i] < 0{
            nums[i] = -nums[i]
            k--
        }
    }
    //再对新得到的数组进行排序
    sort.Ints(nums)
    //判断是否还有剩余的k
    if k > 0{  //还有剩余的k
        //剩余的k为偶数,不用处理,直接求和
        //剩余的k为奇数,把最小的正数取反
        if k%2 == 1{
            //如果数组里有0,,不用处理
            if nums[0] == 0 {
            }else{
                nums[0] = -nums[0]
            }
        }
    }

    //没有剩余的k -- 不用处理 , 直接求和
    //求和
    sum := 0
    for _, v := range nums{
        sum += v
    }

    return sum
}
```

## [860. 柠檬水找零 - 力扣（LeetCode）](https://leetcode.cn/problems/lemonade-change/submissions/644215609/)

![image-20250715201900055](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202507152019497.png)

```go
func lemonadeChange(bills []int) bool {
    five, ten, twenty := 0,0,0 //手里5, 10, 20的数量

    //遍历数组
    for _,v := range bills{
        //根据付的钱分类
        
        if v == 5{  //付5， 不用找零
            five++
        }else if v == 10{  //付10，找零5
            if five == 0{  //没有5，不能找零
                return false
            }
            //有5
            five-- 
            ten ++
        }else if v == 20{
            if (five == 0 || ten == 0) && five < 3{
                return false
            }
            if ten != 0{
                ten--
                five--
            }else{
                five = five-3
            }
            twenty ++
        }
    }
    return true
}
```

## [406. 根据身高重建队列 - 力扣（LeetCode）](https://leetcode.cn/problems/queue-reconstruction-by-height/submissions/644227253/)

![image-20250715205328475](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202507152053638.png)

```go
//先按照身高降序排序
//再看k，依次把每一个放在下标为k的位置（前面有k个比它高，也就是他是第k+1个，下标为k）

func reconstructQueue(people [][]int) [][]int {
    //按身高降序排序
    sort.Slice(people, func(i, j int)bool {
        if people[i][0] == people[j][0]{  //身高相同时，k从小到大排
            return people[i][1] < people[j][1]
        }
        return people[i][0] > people[j][0]  //身高从高到低排序
    })

    //再排K，插入到下标为k的位置
    for k, v := range people{ //这里的k代表这是第k个下标
        //v是数组  v[1]就是要插入的下标  
        copy(people[v[1]+1 : k+1], people[v[1]: k+1]) //将后面所有的拷贝 + 扩容，然后再赋值替换
        people[v[1]] = v
    }
    return people
}
```

