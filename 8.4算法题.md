## [452. 用最少数量的箭引爆气球](https://leetcode.cn/problems/minimum-number-of-arrows-to-burst-balloons/) 

![image-20250604163317187](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250604163317187.png) 

```go
//这道题实际上是要寻找一个点同时存在于多个区间，即寻找这种类型的点的最少个数，所以要找到包含尽可能多的这种类型的点，这里我们采用右端点升序排列的方法，如果左端点大于之前区间的右端点，即符合条件
func findMinArrowShots(points [][]int) (ans int) {
    sort.Slice(points,func(i,j int) bool {//自定义升序排序
        return points[i][1]<points[j][1]
    })
    pre:=math.MinInt//初始右端点设置为一个极小值，便于遍历设置右端点值
    for _,p :=range points {
        if p[0]>pre {//如果左端点大于右端点，结果加一
            ans++
            pre=p[1]//设置新的更长区间的右端点
        }
    }
    return
}
```

