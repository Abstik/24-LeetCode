## [128. 最长连续序列](https://leetcode.cn/problems/longest-consecutive-sequence/)

![image-20250528214150058](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250528214150058.png)

```go
//先用哈希表去除重复数字，再遍历哈希表找到连续数字
func longestConsecutive(nums []int) (ans int) {
    hash:=make(map[int]bool)
    for _,v :=range nums {//定义每个不相同的数为true,之后遍历哈希表寻找true的数即可去重
        hash[v]=true
    }
    for x :=range hash {
        if hash[x-1] {//如果x-1也在这个列表中，直接continue，因为从x-1开始获得的序列一定更长
            continue
        }
        y:=x+1//定义y为X的下一个数
        for hash[y] {//寻找y是否在序列中，如果找到则继续寻找下一个数，
            y++
        }
        ans=max(ans,y-x)//y-x即为序列长度(因为y++使y指向下一个空位，所以相当于最远的数减去最近的数加一)，取最大的长度值
    }
    return 
}
```

