## [797. 所有可能的路径](https://leetcode.cn/problems/all-paths-from-source-to-target/) 

![image-20250524190806314](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250524190806314.png) 

```go
func allPathsSourceTarget(graph [][]int) (ans [][]int) {
    //整体思路：graph[i]的索引即为一条路径的起始节点，我们可以从0开始dfs每一个节点，到最后一个节点时将这条路径记录下来，再回退一个节点，看看是否；还能找到其他的路径
    stk :=[]int{0}//题目要求从节点0开始找路径，所以先把0导入
    var dfs func(int)//声明dfs函数
    dfs =func (x int) {
        if x==len(graph)-1 {//如果找到了最后一个节点，说明有一条完整路径，记录下来
            ans=append(ans,append([]int(nil),stk...))
            //运用深拷贝，append([]int(nil), stk...) 的作用是 创建 stk 的副本，避免后续修改影响已存储的路径。[]int(nil) 创建一个空的 int 切片。stk... 将 stk 的所有元素展开，追加到空切片中，相当于复制了一份 stk。
            //为什么需要深拷贝？如果直接 append(ans, stk)，ans 里存储的是 stk 的引用，后续 stk 被修改时，ans 里的路径也会被修改。使用 append([]int(nil), stk...) 确保 ans 存储的是独立的路径副本，不受后续 stk 变化的影响。
            return 
        }
    for _,y :=range graph[x] {//遍历当前节点x的所有邻居节点y
        stk=append(stk,y)
        dfs(y)//递归调用DFS继续探索从y出发的路径
        stk=stk[:len(stk)-1]//回溯：将y从栈中弹出（移除当前路径的最后一个节点），尝试其他邻居
    }
    }
    dfs(0)//从初始节点0开始执行DFS
    return
}
```

