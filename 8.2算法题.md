## [406. 根据身高重建队列](https://leetcode.cn/problems/queue-reconstruction-by-height/)

![image-20250602214849866](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250602214849866.png) 

```go
//这道题根据贪心思维我们先要判断先排序身高还是k值，我们采用贪心策略：使当前正在处理的对象，对其有影响的人最少。所以我们应当先对身高进行排序，身高低的人对比他身高更高的人没有影响。因此，我们优先处理身高较高的人，这样在后续处理身高更低的人时，对当前对象没有影响，
func reconstructQueue(people [][]int) [][]int {//自定义排序函数，func(i, j int) 是一个 匿名函数（anonymous function），它作为 sort.Slice 的第二个参数，用于定义排序的规则。
    sort.Slice(people,func(i,j int) bool {
        if people[i][0]==people[j][0] {//当两个人的身高 h 相同时，k 较小的应该排在前面，因为：k 较小的人前面允许的高个子更少，所以需要先放置他们，避免后面插入时影响他们的 k 值。
            return people[i][1]<people[j][1]//按 k 升序排列：return true → people[i] 排在 people[j] 前面，return false → people[j] 排在 people[i] 前面
        }else {
            return people[i][0]>people[j][0]//按 h 降序排列：高个子先处理，这样后面插入矮个子时，不会影响高个子的 k 值（因为矮个子不会增加高个子前面的更高人数）。
        }
    })
    queue:=make([][]int,len(people))
    for i,val :=range people {
        tmp:=append([][]int{},queue[val[1]:i]...)/*保存 queue 中 从 val[1] 到 i 的所有元素，以便后续重新插入。
val[1] 是当前人的 k 值（前面应有 k 个更高或等高的人）。
queue[val[1]:i]：
从 queue 的 val[1] 位置开始，到 i 位置结束（切片）。
由于 queue 初始长度和 people 相同（make([][]int, len(people))），但刚开始时 queue 是空的（全零值），所以 queue[val[1]:i] 可能包含 nil 或空切片。
append([][]int{}, ...)：
创建一个新的临时切片 tmp，存储 queue[val[1]:i] 的元素，避免直接修改 queue 导致错误
为什么需要 tmp？
因为我们要在 queue 的 val[1] 位置插入当前人 val，但 queue[val[1]:] 后面的元素需要保留。
tmp 存储了这些需要保留的元素，以便插入 val 后再拼接回来。*/
        queue=append(queue[:val[1]],val)/*queue = append(queue[:val[1]], val)
作用：将当前人 val 插入到 queue 的 val[1] 位置。
queue[:val[1]]：
取 queue 的前 val[1] 个元素（即 queue[0..val[1]-1]）。
append(queue[:val[1]], val)：
把 val 追加到 queue[:val[1]] 后面，相当于在 val[1] 位置插入 val。
为什么可以直接插入 val[1] 位置？
因为 people 已经按 h 降序排序，当前 val 的身高 h ≤ 前面所有人的 h。
所以 val 的插入不会影响前面已经放置的人的 k 值（因为 val 不会比他们高）。*/
        queue=append(queue,tmp...)//将之前保存的 tmp（queue[val[1]:i] 的元素）重新接在 queue 后面。
    }
    return queue
}
```



