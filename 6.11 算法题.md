## [496. 下一个更大元素 I](https://leetcode.cn/problems/next-greater-element-i/)

![image-20250611152140709](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250611152140709.png)



~~~go
func nextGreaterElement(nums1 []int, nums2 []int) []int {
	rt := make([]int, len(nums1))
	for i := range rt {
		rt[i] = -1
	}
	mp := map[int]int{}
	for i, v := range nums1 {
		mp[v] = i
	}
	// 模拟单调栈
	stack := []int{}
	stack = append(stack, 0)

	for i := 1; i < len(nums2); i++ {
		for len(stack) > 0 && nums2[i] > nums2[stack[len(stack)-1]] {
			top := stack[len(stack)-1]

			if _, ok := mp[nums2[top]]; ok { // 看map里是否存在这个元素
				index := mp[nums2[top]] // 根据map找到nums2[top]在nums1中的下标
				rt[index] = nums2[i]
			}
			stack = stack[:len(stack)-1] // 出栈
		}
		stack = append(stack, i)
	}
	return rt
}
~~~



## [503. 下一个更大元素 II](https://leetcode.cn/problems/next-greater-element-ii/)

![image-20250611152407615](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250611152407615.png)



~~~go
func nextGreaterElements(nums []int) []int {
	num := make([]int, len(nums)*2)
	copy(num, nums)
	copy(num[len(nums):], nums)
	// 用新的nums大小来初始化rt
	rt := make([]int, len(num))
	for i := range rt {
		rt[i] = -1
	}
	// 单调栈
	st := []int{0}
	for i := 1; i < len(num); i++ {
		if num[i] < num[st[len(st)-1]] {
			st = append(st, i)
		} else if num[i] == num[st[len(st)-1]] {
			st = append(st, i)
		} else {
			for len(st) > 0 && num[i] > num[st[len(st)-1]] {
				rt[st[len(st)-1]] = num[i]
				st = st[:len(st)-1]
			}
			st = append(st, i)
		}
	}
	rt = rt[:len(rt)/2]
	return rt
}
~~~

