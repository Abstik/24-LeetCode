## [77. 组合](https://leetcode.cn/problems/combinations/)

![image-20250525184435405](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250525184435405.png)



```go
var (
	path []int
	rt   [][]int
)

func combine(n int, k int) [][]int {
	path, rt = make([]int, 0, k), make([][]int, 0)
	dfs(n, k, 1)
	return rt
}

func dfs(n int, k int, start int) {
	if len(path) == k { // 说明已经满足了k个数的要求
		tmp := make([]int, k)
		copy(tmp, path)
		rt = append(rt, tmp)
		return
	}
	for i := start; i <= n; i++ { // 从start开始，不往回走，避免出现重复组合
		if n-i+1 < k-len(path) { // 剪枝
			break
		}
		path = append(path, i)
		dfs(n, k, i+1)
		path = path[:len(path)-1]
	}
}
```



## [216. 组合总和 III](https://leetcode.cn/problems/combination-sum-iii/)

![image-20250525185419595](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250525185419595.png)



```go
var (
	rt   [][]int // 存储所有符合条件的答案
	path []int   // 当前搜索路径
)

func combinationSum3(k int, n int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, k)
	dfs(k, n, 1, 0)
	return rt
}

func dfs(k, n int, start int, sum int) {
	// 当路径长度达到k时，检查当前和是否等于目标值n
	if len(path) == k {
		if sum == n {
			// 复制当前路径
			tmp := make([]int, k)
			copy(tmp, path)
			rt = append(rt, tmp)
		}
		return
	}

	for i := start; i <= 9; i++ {
		// 剪枝
		// 1. 若当前数字加入后和超过n，后续更大的数字无需再试
		// 2. 若剩余可选数字不足所需数量(k-len(path))，提前终止循环
		if sum+i > n || 9-i+1 < k-len(path) {
			break
		}

		path = append(path, i)
		dfs(k, n, i+1, sum+i)
		// 回溯，尝试其他可能
		path = path[:len(path)-1]
	}
}
```



