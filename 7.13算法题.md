## [763. 划分字母区间](https://leetcode.cn/problems/partition-labels/) 

![image-20250604163331697](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250604163331697.png)

```go
//题意为同一个字母必须在同一个片段里，所以我们应寻找每个字母的最远位置，在遍历过程中不断更新，直到遍历到一个最远位置为一个片段
func partitionLabels(s string) (ans []int) {
    letter:=make(map[rune]int)//这里要注意，字母是rune类型
    for i,v :=range s {
        letter[v-'a']=i//遍历存储每一个字母的索引，遍历完之后就存储了每个字母的最远位置
    }
    start,end :=0,0//开头和结束
    for i,v :=range s {
        if letter[v-'a']>end {//更新最远位置
            end=letter[v-'a']
        }
        if i==end {
            ans=append(ans,end-start+1)//得到一个片段长度
            start=end+1//更新下一个片段的开头
        }
    }
    return
}
```

