## [669. 修剪二叉搜索树 - 力扣（LeetCode）](https://leetcode.cn/problems/trim-a-binary-search-tree/submissions/635646466/)

![image-20250609223901904](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202506092239014.png)

```go
//不在要求范围内的结点删除
//小于最小 -- 它的左子树都不符合要求，都没了，不用管，修建右子树
//大于最大 -- 它的右子树都不符合要求

func trimBST(root *TreeNode, low int, high int) *TreeNode {
    if root == nil{
        return nil
    }

    if root.Val < low{
        return trimBST(root.Right, low, high)
    }
    if root.Val > high{
        return trimBST(root.Left, low, high)
    }

    //root符合要求，修理它的左子树和右子树
    root.Left = trimBST(root.Left, low, high) 
    root.Right = trimBST(root.Right, low, high)

    return root
}
```

