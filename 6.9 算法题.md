## [15. 三数之和](https://leetcode.cn/problems/3sum/)

![image-20250609154813193](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250609154813193.png)



~~~go
func threeSum(nums []int) [][]int {
	rt := make([][]int, 0)
	sort.Ints(nums)
	// a = nums[i], b = nums[j], c = -(a + b)
	for i := 0; i < len(nums); i++ {
		// 排序之后如果第一个元素大于零，那么不能凑成三元组
		if nums[i] > 0 {
			break
		}
		// 三元组元素a去重
		if i > 0 && nums[i] == nums[i-1] {
			continue
		}
		set := make(map[int]struct{})
		for j := i + 1; j < len(nums); j++ {
			// 三元组元素b去重
			if j > i+2 && nums[j] == nums[j-1] && nums[j-1] == nums[j-2] {
				continue
			}
			c := -nums[i] - nums[j]
			if _, ok := set[c]; ok {
				rt = append(rt, []int{nums[i], nums[j], c})
				// 三元组元素c去重
				delete(set, c)
			} else {
				set[nums[j]] = struct{}{}
			}
		}
	}
	return rt
}
~~~



## [18. 四数之和](https://leetcode.cn/problems/4sum/)

![image-20250609155051717](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250609155051717.png)



~~~go
func fourSum(nums []int, target int) [][]int {
	if len(nums) < 4 {
		return nil
	}
	sort.Ints(nums)
	var rt [][]int
	for i := 0; i < len(nums)-3; i++ {
		n1 := nums[i]
		if i > 0 && n1 == nums[i-1] {
			continue // nums[i]去重
		}
		for j := i + 1; j < len(nums)-2; j++ {
			n2 := nums[j]
			if j > i+1 && n2 == nums[j-1] {
				continue // nums[j]去重
			}
			l := j + 1
			r := len(nums) - 1
			for l < r {
				n3 := nums[l]
				n4 := nums[r]
				sum := n1 + n2 + n3 + n4
				if sum < target {
					l++
				} else if sum > target {
					r--
				} else {
					rt = append(rt, []int{n1, n2, n3, n4})
					for l < r && n3 == nums[l+1] {
						l++
					}
					for l < r && n4 == nums[r-1] {
						r--
					}
					r--
					l++
				}
			}
		}
	}
	return rt
}
~~~

