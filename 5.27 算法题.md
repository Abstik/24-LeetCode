## [40. 组合总和 II](https://leetcode.cn/problems/combination-sum-ii/)

![image-20250527190118980](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250527190118980.png)



```go
var (
	rt   [][]int
	path []int
)

func combinationSum2(candidates []int, target int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, len(candidates))
	sort.Ints(candidates) // 排序，为剪枝做准备
	var dfs func([]int, int, int)
	dfs = func(candidates []int, start int, target int) {
		if target == 0 { // target 不断减小，如果为0说明达到了目标值
			tmp := make([]int, len(path))
			copy(tmp, path)
			rt = append(rt, tmp)
			return
		}
		for i := start; i < len(candidates); i++ {
			if candidates[i] > target { // 剪枝，提前返回
				break
			}
			// i != start 限制了这不对深度遍历到达的此值去重
			if i != start && candidates[i] == candidates[i-1] { // 去重
				continue
			}
			path = append(path, candidates[i])
			dfs(candidates, i+1, target-candidates[i])
			path = path[:len(path)-1]
		}
	}
	dfs(candidates, 0, target)
	return rt
}
```



## [131. 分割回文串](https://leetcode.cn/problems/palindrome-partitioning/)

![image-20250527212351518](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250527212351518.png)



```go
var (
	path []string // 放已经回文的子串
	rt   [][]string
)

func partition(s string) [][]string {
	path, rt = make([]string, 0), make([][]string, 0)
	var dfs func(string, int)
	dfs = func(s string, start int) {
		if start == len(s) { // 如果起始位置等于s的大小，说明已经找到了一组分割方案了
			tmp := make([]string, len(path))
			copy(tmp, path)
			rt = append(rt, tmp)
			return
		}
		for i := start; i < len(s); i++ {
			str := s[start : i+1]
			if judgeFunc(str) { // 是回文子串
				path = append(path, str)
				dfs(s, i+1)               // 寻找i+1为起始位置的子串
				path = path[:len(path)-1] // 回溯过程，弹出本次已经添加的子串
			}
		}
	}
	dfs(s, 0)
	return rt
}

func judgeFunc(s string) bool {
	for i, j := 0, len(s)-1; i < j; i, j = i+1, j-1 {
		if s[i] != s[j] {
			return false
		}
	}
	return true
}
```

