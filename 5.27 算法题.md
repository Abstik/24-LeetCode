## [1035. 不相交的线](https://leetcode.cn/problems/uncrossed-lines/submissions/632871124/)

![image-20250527222558810](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202505272225213.png)

```go
//nums1[i]和nums2[j]相连
//nums1[i+1]和nums2[k]相连 -- k > j
//两个数组顺序相同 -- 相同子序列

func maxUncrossedLines(nums1 []int, nums2 []int) int {
    len1 := len(nums1)
    len2 := len(nums2)
    dp := make([][]int, len1+1)
    for i:=range dp{
        dp[i] = make([]int, len2+1)
    }

    for i,v1 := range nums1{
        for j,v2 := range nums2{
            if v1 == v2{
                dp[i+1][j+1] = dp[i][j] + 1
            }else{
                dp[i+1][j+1] = max(dp[i][j+1], dp[i+1][j])
            }
        }
    }
    return dp[len1] [len2]
}

func max(x,y int)int{   //返回最大值
    if x>y{
        return x
    }
    return y
}
```

