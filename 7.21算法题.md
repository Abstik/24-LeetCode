## [738. 单调递增的数字](https://leetcode.cn/problems/monotone-increasing-digits/) 

![image-20250609183605930](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250609183605930.png) 



```go
//这里我们不能用单纯的数字一直减一来找到符合条件的数，因为会超时，所以要想更高效的方法，用贪心回退 + 补9法，从左到右扫描，如果某一位比后一位大：把这一位减 1（即回退），后面的所有位都变成 9（因为变大就超过原数，只能最大化地变成 9）从右向左找违背单调的位置，记录修改点，最后从修改点开始补九就可以找到最大的符合条件的数
func monotoneIncreasingDigits(n int) int {
    digits:=[]byte(strconv.Itoa(n))//这里利用byte类型的字节切片来存储转换的字符，注意不能写成digits := strconv.Itoa(n)，因为两者类型不同，digits:=[]byte(strconv.Itoa(n))的类型是byte,而digits := strconv.Itoa(n)的类型是string,因为后面这段代码要修改数字中的某些位，这在 Go 中，只有 []byte（或 []rune）可以做到，string 是不可变的，你不能直接改某一位。
    sign:=len(digits)
    for i:=len(digits)-1;i>0;i-- {//从后往前遍历数组，找不符合递增的数并标记补九位
        if digits[i-1]>digits[i] {
            digits[i-1]-- //因为要把第i位置问九，所以第i-1位要回退一个数字
            sign=i
        }
    }
    for i:=sign;i<len(digits);i++ {//从标记位开始补九
        digits[i]='9'
    }
    ans,_ :=strconv.Atoi(string(digits))
    /*标准库 strconv.Atoi 的签名是：func Atoi(s string) (int, error)，返回的是 两个值：int 类型的结果，error 类型的错误，可以写成ans, err := strconv.Atoi(string(digits))，或者ans, _ := strconv.Atoi(string(digits))，丢弃 error，strconv.Atoi() 定义：func Atoi(s string) (int, error)，它接受的是一个字符串类型 string，不是字节切片 []byte，必须将 []byte 转为 string，否则会编译报错*/
    return ans
}
```

