## [15. 三数之和](https://leetcode.cn/problems/3sum/)  

![image-20250525195442762](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250525195442762.png) 

```go
//用双指针的解法，注意要先对数组进行排序，这是双指针法的前提，这里分别定义三个指针，表示三个数，然后按循环计算即可
func threeSum(nums []int) [][]int {
    n :=len(nums)
    sort.Ints(nums) 
    ans :=make([][]int,0)//初始化
    for first:=0;first<n;first++ {//外层循环用于定义第一个数
        if first>0&& nums[first]==nums[first-1] {//不能有重复的数出现
            continue
        }
        third :=n-1//这里定义第三个数的指针，因为要在内层循环计算数是否相等
        sum :=-1*nums[first]//b+c=-a,这样方便计算
        for second:=first+1;second<n;second++ {//内层循环定义第二个数
            if second>first+1 && nums[second]==nums[second-1] {//不能重复
                continue
            }
            for third>second && nums[second]+nums[third]>sum {//第三个数从最后一个开始计算，且必须在第二个指针后面，如果比目标值大，指针往前移动
                third--
            }
            if second==third {//如果第二个指针和第三个指针一样大，没有必要再计算了，因为第二个指针只会越来越大，不符合条件，跳到最外层循环寻找新的第一个数计算
                break
            }
            if nums[second]+nums[third]==sum {//符合条件加入
                ans=append(ans,[]int{nums[first],nums[second],nums[third]})
            }
        }
    }
    return ans 
}  

```

