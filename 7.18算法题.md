## [643. 子数组最大平均数 I](https://leetcode.cn/problems/maximum-average-subarray-i/) 

![image-20250609180529822](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250609180529822.png) 



```go
//二分查找的思想,但用标准模板会超时，这里可以先把k个数的和算出来，再进行一层循环窗口的滑动
func findMaxAverage(nums []int, k int) float64 {
    sum:=0
    for i:=0;i<k;i++ {
        sum+=nums[i]
    }
    maxsum:=sum//获取最大值比较
    for i:=k;i<len(nums);i++ {
        sum+=nums[i]-nums[i-k]//滑动窗口右移动
        if sum>maxsum {
            maxsum=sum
        }
    }
    return float64(maxsum)/float64(k)//强制类型转换
}
```

