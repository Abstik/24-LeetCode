## [53. 最大子数组和](https://leetcode.cn/problems/maximum-subarray/) 

![image-20250526195012352](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250526195012352.png) 

```go
//这里我们可以从数组开头开始，先将第一个数组元素设为最大值，之后不断改变最大值，可以将当前元素和与前一个元素相加看是否比当前元素大来保持连续性
func maxSubArray(nums []int) int {
    max:=nums[0]
    for i:=1;i<len(nums);i++ {//先将第一个元素设置为最大值，这里从第一个开始遍历
        if nums[i]+nums[i-1]>nums[i] {//因为我们是从第一个开始的，所以这里我们判断当前元素之和和前一个元素相加是否比当前元素和大，如果比当前元素大，相加，表示元素之和说明还有更大的和，这里也可以保持连续性，如果小于当前元素和，即连续性已断，
            nums[i]+=nums[i-1]
        }
        if nums[i]>max {//更新最大元素和
            max=nums[i]
        }
    }
    return max
}
```

