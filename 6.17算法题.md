## [654. 最大二叉树](https://leetcode.cn/problems/maximum-binary-tree/)

![image-20250524200235649](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250524200235649.png) 

```go
//运用递归的思想，先找到最大值，再锁定最大值再数组中的位置，获取左边界以便获取左右子树，再递归左右子树创建二叉树
func findmax(nums []int) int {//遍历数组找最大值，这样可以不改变数组
    max :=nums[0]
    for _,num :=range nums {
        if num>max {
            max=num
        }
    }
    return max
}
func constructMaximumBinaryTree(nums []int) *TreeNode {
    n:=len(nums)
    if n==0 {//当数组中没有值了，说明该节点值为空，返回nil
        return nil
    }
    max :=findmax(nums) //通过函数找到最大值
    leftSize :=slices.Index(nums,max)//使用slices.Index函数找到最大值在切片中的具体位置并设置为左边界
    left :=constructMaximumBinaryTree(nums[:leftSize])//这里要注意go语言中切片再切片时[a:b]，a是包含的而b是不包含的，所以这里的具体索引表示从0到leftSize减一，正好表示左子树的范围
    Right :=constructMaximumBinaryTree(nums[leftSize+1:])//由于leftSize是最大值，也就是根节点的具体位置，所以右子树应该从leftSize+1开始
    return &TreeNode{max,left,Right}//最后返回根节点值左子树指向递归构建的left，右子树指向递归构建的right
}
```

