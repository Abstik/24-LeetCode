## [77. 组合](https://leetcode.cn/problems/combinations/) 

![image-20250609174136069](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250609174136069.png) 

```go
//回溯的思想，从一回溯到n
func combine(n int, k int) (ans [][]int) {
    path :=[]int{}//创建空切片数组存储数字，也可以写为make([]int,0,k);长度一定要为0，因为之后是通过append添加数且通过判断数组长度是否等于k来判断递归结束
    var dfs func(start int)
    dfs = func(start int) {
        if len(path) == k {
            ans=append(ans,append([]int(nil),path...))//深拷贝防止结果数组被修改
            return
        }
        for i := start; i <= n; i++ {//索引赋值为start，防止重复添加
            path = append(path, i)//修改状态
            dfs(i + 1)//递归      
            path = path[:len(path)-1]//恢复现场  
        }
    }

    dfs(1)
    return
}

```

