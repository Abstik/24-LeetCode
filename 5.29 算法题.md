## [72. 编辑距离 ](https://leetcode.cn/problems/edit-distance/)

![image-20250529222813337](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202505292228449.png)

```go
//插入删除替换
//dp[i][j] 不操作
//dp[i-1][j] 删除word1字符
//dp[i][j-1]表示在word1插入字符
//dp[i-1][j-1]表示替换word1一个字符

func minDistance(word1 string, word2 string) int {
    m := len(word1)   //长度
    n := len(word2) 
    
    dp := make([][]int,m+1)
    for i := 0; i < m+1;i++{
        dp[i] = make([]int,n+1)
        dp[i][0] = i
    }

    for j := 0; j < n+1;j++{
        dp[0][j] = j  
    }

    for i := 1;i < m+1;i++{
        for j := 1; j < n+1;j++{
            if word1[i-1] == word2[j-1]{
                //相同 不操作
                dp[i][j] = dp[i-1][j-1]
            }else{

                dp[i][j] = min(dp[i-1][j],min(dp[i][j-1],dp[i-1][j-1])) + 1 
            }
        }
    }
    return dp[m][n]
}

func min(a,b int)int{
    if a > b{
        return b 
    }
    return a 
}
```

