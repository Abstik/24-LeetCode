## [90. 子集 II](https://leetcode.cn/problems/subsets-ii/)

![image-20250529185026263](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250529185026263.png)



```go
var (
	path []int
	rt   [][]int
)

func subsetsWithDup(nums []int) [][]int {
	path, rt = make([]int, 0, len(nums)), make([][]int, 0)
	sort.Ints(nums) // 排序,便于去重

	var dfs func([]int, int)
	dfs = func(nums []int, start int) {
		tmp := make([]int, len(path))
		copy(tmp, path)
		rt = append(rt, tmp)

		for i := start; i < len(nums); i++ {
			// 当i不是起始位置，且当前元素和前一个元素相同，则跳过，避免重复
			if i != start && nums[i] == nums[i-1] {
				continue
			}
			path = append(path, nums[i])
			dfs(nums, i+1)            // 递归处理剩余元素
			path = path[:len(path)-1] // 回溯，移除最后添加的元素
		}
	}
	dfs(nums, 0)
	return rt
}
```



## [491. 非递减子序列](https://leetcode.cn/problems/non-decreasing-subsequences/)

![image-20250529185422771](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250529185422771.png)



```go
var (
	rt   [][]int
	path []int
)

func findSubsequences(nums []int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, len(nums))
	var dfs func([]int, int)
	dfs = func(nums []int, start int) {
		if len(path) >= 2 {
			tmp := make([]int, len(path))
			copy(tmp, path)
			rt = append(rt, tmp)
		}
		used := make(map[int]bool, len(nums)) // 初始化used，用以对同层元素去重
		for i := start; i < len(nums); i++ {
			if used[nums[i]] { // 去重
				continue
			}
			if len(path) == 0 || nums[i] >= path[len(path)-1] {
				path = append(path, nums[i])
				used[nums[i]] = true
				dfs(nums, i+1)
				path = path[:len(path)-1]
			}
		}
	}
	dfs(nums, 0)
	return rt
}
```

