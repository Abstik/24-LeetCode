## [454. 四数相加 II](https://leetcode.cn/problems/4sum-ii/)

![image-20250608115712579](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250608115712579.png)



~~~go
func fourSumCount(nums1 []int, nums2 []int, nums3 []int, nums4 []int) int {
	n := make(map[int]int)
	cnt := 0

	// 构建nums1和nums2的和的map
	for _, v1 := range nums1 {
		for _, v2 := range nums2 {
			n[v1+v2]++
		}
	}

	// 遍历nums3和nums4，检查-c-d是否在map中
	for _, v3 := range nums3 {
		for _, v4 := range nums4 {
			sum := -v3 - v4
			if num, ok := n[sum]; ok {
				cnt += num
			}
		}
	}
	
	return cnt
}
~~~



## [383. 赎金信](https://leetcode.cn/problems/ransom-note/)

![image-20250608115907176](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250608115907176.png)



~~~go
func canConstruct(ransomNote string, magazine string) bool {
	cal := make([]int, 26)
	// 通过record数据记录 magazine里各个字符出现次数
	for _, v := range magazine {
		cal[v-'a']++
	}
	// 遍历ransomNote，在record里对应的字符个数做--操作
	for _, v := range ransomNote {
		cal[v-'a']--
		// 如果小于零说明ransomNote里出现的字符，magazine没有
		if cal[v-'a'] < 0 {
			return false
		}
	}
	return true
}
~~~

