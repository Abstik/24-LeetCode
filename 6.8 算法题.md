## [718. 最长重复子数组 ](https://leetcode.cn/problems/maximum-length-of-repeated-subarray/submissions/635405536/)

![image-20250608221911679](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202506082219090.png)

```go
//dp[i][j] ： nums1的0-(i-1)个，nums2的0-(j-1)个，最长重复
//nums1[i] = nums2[j] -> dp[i][j] = dp[i-1][j-1] + 1
//nums1[i] != nums2[j] -> dp[i][j] = 0 //必须连续的,如果不相同就在这里中断了

func findLength(nums1 []int, nums2 []int) int {
    len1 := len(nums1)
    len2 := len(nums2)

    //初始化dp（第一行和第一列）
    //把dp向外扩张一层，都为0
    dp := make([][]int, len1 + 1)
    for i := range dp{
        dp[i] = make([]int, len2+1)
    }

    //遍历nums1和nums2
    for i,v1 := range nums1{
        for j, v2 := range nums2{
            if v1 == v2{
                dp[i+1][j+1] = dp[i][j] + 1
            }else{
                dp[i+1][j+1] = 0
            }
        }
    }

    max := math.MinInt

    for _, v1 := range dp{
        for _, v2 := range v1{
            if v2 > max{
                max = v2
            }
        }
    }

    return max
}

func max(x, y int)int{
    if x > y{
        return x
    }
    return y
}
```

## [583. 两个字符串的删除操作 - 力扣（LeetCode）](https://leetcode.cn/problems/delete-operation-for-two-strings/submissions/635408640/)

![image-20250608223329924](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202506082233325.png)

```go
//只有删除与不删除的状态

//dp[i][j] : word1的从0到i-1，word2的从0到j-1
//word1[i] = word2[j] -> dp[i][j] = dp[i-1][j-1]
//word1[i] != word2[j] -> dp[i][j] = min(dp[i-1][j] + 1 , dp[i][j-1] + 1, dp[i-1][j-1] + 2)  //表示删除word1或者word2或者都删除

func minDistance(word1 string, word2 string) int {
    len1 := len(word1)
    len2 := len(word2)

    //初始化dp
    dp := make([][]int, len1+1)
    for i := range dp{
        dp[i] = make([]int, len2+1)
        dp[i][0] = i
    }
    for j := 0; j < len2+1; j ++{
        dp[0][j] = j
    }


    //dp
    for i, v1 := range word1{
        for j, v2 := range word2{
            if v1 == v2{
                dp[i+1][j+1] = dp[i][j]
            }else {
                dp[i+1][j+1] = min(dp[i][j+1] + 1, dp[i+1][j] + 1, dp[i][j] + 2)
            }
        }
    }

    return dp[len1][len2]  
}


func min(x, y, z int)int{
    if x < y{
        if x < z{
            return x
        }
        return z
    }else {
        if y < z{
            return y
        }
        return z
    }
}
```

