## [17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number/) 

![image-20250613203515760](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250613203515760.png)

```go
//这里我们要知道如何用数字调用到其中的字母，这里利用一个哈希表的办法调用里面的字母，然后再回溯，对于这里是字符组合问题，可以用隐式回溯的办法
func letterCombinations(digits string) (ans []string) {
    mapnum:=[...]string{"","","abc","def","ghi","jkl","mno","pqrs","tuv","wxyz"}//先将各个号码的字符存储在map中，注意0和1索引没有值，为空
    n:=len(digits)//获取所给的数字字符的长度，如果没有，说明为空，那么结果也是空的，直接返回
    if n==0 {
        return
    }
    path:=make([]byte,n)//这里建立byte的切片，便于修改
    var dfs func(int)
    dfs=func(i int){
        if i==n {//当数字索引与数字字符的长度相等时，说明已有结果
            ans=append(ans,string(path))//这里不是深拷贝，但把 []byte 类型的 path 转换为了 string。在 Go 中，[]byte 转 string 的时候会进行一次内存拷贝，生成的是新的字符串数据，与原来的 path 切片无任何引用关系。即使之后你修改了 path 的内容，已经 append 到 ans 中的字符串也不会受到影响。不需要手动深拷贝，原因是 string(path) 已经是深拷贝后的新对象了
            return
        }
        for _,c :=range mapnum[digits[i]-'0'] {//通过数字索引找到map中的对应串
            path[i]=byte(c)//隐式回溯，因为每次递归返回以后，上一层还会继续 for 循环，覆盖同一个 path[i]，所以之前的赋值会被新的值覆盖，自然达到“撤销选择”的效果。
            /*观察关键点：
path 是固定长度 n 的切片。
每一层递归只操作 path[i] 这一位。
下一层递归只关心 path[i+1]，而不会动之前的位。
回溯的本质在这里体现为：
并不是“弹出最后一步”，而是 “覆盖当前选择”。
每一层只控制它自己的位置 path[i]，不会动别的。
所以每层 DFS 结束后，path 的状态自动就“退回”到了上一步的状态（因为我们只修改当前层的位）。*/
            dfs(i+1)
        }
    }
    dfs(0)
    return
}
```

