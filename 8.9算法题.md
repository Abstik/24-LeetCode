## [LCR 083. 全排列](https://leetcode.cn/problems/VvJkup/) 

![image-20250609180518921](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250609180518921.png) 

```go
//同样是回溯，与组合那道题进行对比，这道题判断的是数组里的数的排列方式，没有规律可循，但是返回的长度与数组长度一致，所以要想办法判断数组里的数是否被用过，而不是单纯的循环，这里采用设置一个bool数组的判断方式，利用bool值和索引来添加数
func permute(nums []int) (ans [][]int) {
    n:=len(nums)
    path:=make([]int,n)//不能写成path:=[]int{},因为空切片不能通过下标赋值，一定要初始化长度，下面checkpath是一样的
    checkpath:=make([]bool,n)//初始化为[]bool{false, false, false, ..., false} // 长度为 n，全是 false
    var dfs func (int)
    dfs=func (x int) {
        if x==n {
            ans=append(ans,append([]int(nil),path...))//深拷贝防止数组被修改
            return
        }
        for i,ok :=range checkpath {
            if ok==false {//如果该值没有被添加过，赋值
                path[x]=nums[i]
                checkpath[i]=true//更改状态
                dfs(x+1)//递归
                checkpath[i]=false//恢复现场，回溯
            }
        }
    }
    dfs(0)//从第0个位置开始
    return
}
```

