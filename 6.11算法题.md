## [513. 找树左下角的值](https://leetcode.cn/problems/find-bottom-left-tree-value/)

![image-20250523221844887](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250523221844887.png) 

```go 
func findBottomLeftValue(root *TreeNode) int {
    //整体思路：运用层序遍历，先将右子节点入队，再将左子节点入队，再分别出队，最后层序遍历完之后最后一个出队的节点即为符合条件的节点。
    if root==nil {//如果根节点为空，返回0
        return 0
    }
    node :=root//用于获取当前层的节点，这里表示从根节点开始遍历
    queue :=[]*TreeNode{root}//创建队列并将根节点入队
    for len(queue)>0 {
        node=queue[0]//获取当前节点并出队
        queue=queue[1:]
            if node.Right !=nil {//获取下一层节点
                queue=append(queue,node.Right)
            }
            if node.Left !=nil {
                queue=append(queue,node.Left)
            }
    }
    return node.Val
}
```

