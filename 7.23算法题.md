## [376. 摆动序列](https://leetcode.cn/problems/wiggle-subsequence/) 

![image-20250525213335422](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250525213335422.png) 

```go
//贪心思想，先判断长度是否小于二，注意，数组中没有元素或者只有一个元素也是摆动序列，再定义两个差值，一个用于存储之前的差，一个用于存储当前差
func wiggleMaxLength(nums []int) int {
    n:=len(nums) 
    if n<2 {//如果长度小于二，直接返回即可
        return n
    }
    ans :=1
    sub :=nums[1]-nums[0]
    if sub>0 {//如果数组中的前两个元素之差大于0，符合条件，结果设置为二
        ans=2
    }
    for i:=2;i<n;i++ {//之前已经判断了前两个数，这里从二开始
        newSub :=nums[i]-nums[i-1]//新的差
        if sub>0&&newSub<0 || sub<0&&newSub>0 {//判断
            ans++
            sub=newSub
        }
    }
    return ans
}
```

