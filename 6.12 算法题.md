## [322. 零钱兑换 - 力扣（LeetCode）](https://leetcode.cn/problems/coin-change/)

![image-20250612222735696](https://ting2.oss-cn-beijing.aliyuncs.com/picture/202506122227138.png)

```go
const noResolver int = -1

func coinChange(coins []int, amount int) int {
    // 如果输入的金额为0的话, 返回无解
	if amount < 0 {
		return noResolver
	}
	// dp[index] 代表当amount == index时的解
	// dp[0] == 0
	dp := make([]int, amount+1)
	for index := range dp {
		// 当前index下的解默认值为-1
		currentCount := noResolver
		// 跳过index == 0 的情况
		if index == 0 {
			continue
		}
		// 依次去掉一个***后, 求子集的解; 注意dp的定义, 此时的index等效于amount, 所以去掉某个***coin后的子集的下标是index-coin
		for _, coin := range coins {
			preIndex := index - coin
			// 当前的amount无法用所有***中的任何一个表示
			if preIndex < 0 {
				continue
			}
			// 子集的解
			preCount := dp[preIndex]

			// 当前***代表的子集无解
			if preCount == noResolver {
				continue
				// 根据子集的结果优化当前解
			} else if (currentCount == noResolver) || (currentCount > preCount+1) {
				currentCount = preCount + 1
			}
		}
		dp[index] = currentCount
	}
	return dp[amount]
}
```

