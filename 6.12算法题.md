## [112. 路径总和](https://leetcode.cn/problems/path-sum/)

![image-20250524200212879](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250524200212879.png) 

```go
func hasPathSum(root *TreeNode, targetSum int) bool {
    //整体思路：创建两个队列，队列一用于存储待处理节点，队列二用于存储对应节点到当前节点的路径和
    /*如何避免重复计算节点值的
    创建的队列二为一个切片，包含每一条路径到当前节点之前的节点值之和
    每次处理节点时，直接从队列二取出预先计算好的路径和，而不是重新从根节点累加。*/
    if root==nil {//没有节点不符合条件
        return false
    }
    queue1 :=[]*TreeNode{root}//队列一用于存储节点
    queue2 :=[]int{root.Val}//队列二用于存储路径和
    for len(queue1)>0 {//层序遍历，因为队列一是存储节点的，所以当队列一为空的时候就遍历完了
        curnode :=queue1[0]//分别获取当前的节点和到该节点之前节点的路径之和，并出队
        queue1=queue1[1:]
        curval :=queue2[0]
        queue2=queue2[1:]
        if curnode.Left==nil && curnode.Right==nil {//如果已经遍历到了叶子节点，说明已经走到了这条路径的终点，看看是否与目标和相等
            if curval==targetSum {
                return true
            }
        }
        if curnode.Left!=nil {//加入下一层的节点
            queue1=append(queue1,curnode.Left)
            queue2=append(queue2,curnode.Left.Val+curval)//如果下一层还有节点，将到该节点之前节点的路径和加上该节点的值为新的路径之和，加入队列二，这样可以防止重复计算
        }
        if curnode.Right!=nil {
            queue1=append(queue1,curnode.Right)
            queue2=append(queue2,curnode.Right.Val+curval)
        }
    }
    return false //如果没有符合条件的路径，返回false
}
```

