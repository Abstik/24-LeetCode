## [202. 快乐数](https://leetcode.cn/problems/happy-number/) 

![image-20250525195435535](https://gggzxh.oss-cn-beijing.aliyuncs.com/img/20250525195435535.png) 

```go
//使用哈希表的算法思想，这里我们用哈希表表示一个数计算完之后如果不为一退出循环
func getSum(n int) int {//分解目标数n得到乘积和
    sum :=0
    for n>0 {
        sum+=(n%10)*(n%10)
        n/=10
    }
    return sum
}
func isHappy(n int) bool {
    m :=make(map[int]bool) //定义哈希表
    for n!=1 && m[n]!=true {
        //如果n得到一了可以退出循环，但是如果一直得不到一，将每一次分解的数放在哈希表中，用true表示已经分解过，如果一直得不到一，之后一定会再次得到原来的乘积和，这样就可以退出循环。
        n,m[n]=getSum(n),true
    }
    return n==1
}
```

