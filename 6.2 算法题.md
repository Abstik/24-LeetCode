## [46. 全排列](https://leetcode.cn/problems/permutations/)

![image-20250602191610506](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250602191610506.png)



```go
var (
	rt   [][]int
	path []int
	st   []bool
)

func permute(nums []int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, len(nums))
	// 初始化状态数组，用于标记元素是否已被使用
	st = make([]bool, len(nums))

	var dfs func([]int, int)
	dfs = func(nums []int, cur int) {
		// 当前路径长度等于输入数组长度，则找到了一个完整排列
		if cur == len(nums) {
			// 复制当前路径到临时切片，避免后续修改影响结果
			tmp := make([]int, len(path))
			copy(tmp, path)
			// 将该排列添加到结果集中
			rt = append(rt, tmp)
			return
		}

		for i := 0; i < len(nums); i++ {
			if !st[i] {
				path = append(path, nums[i])
				st[i] = true
				dfs(nums, cur+1)
				// 回溯，标记该元素未被使用
				st[i] = false
				// 从路径中移除最后一个元素
				path = path[:len(path)-1]
			}
		}
	}
	dfs(nums, 0)
	return rt
}
```



## [47. 全排列 II](https://leetcode.cn/problems/permutations-ii/)

![image-20250602191844062](C:\Users\18353\AppData\Roaming\Typora\typora-user-images\image-20250602191844062.png)



```go
var (
	rt   [][]int
	path []int
	st   []bool
)

func permuteUnique(nums []int) [][]int {
	rt, path = make([][]int, 0), make([]int, 0, len(nums))
	st = make([]bool, len(nums))
	sort.Ints(nums)
	var dfs func([]int, int)
	dfs = func(nums []int, cur int) {
		if cur == len(nums) {
			tmp := make([]int, len(path))
			copy(tmp, path)
			rt = append(rt, tmp)
		}
		for i := 0; i < len(nums); i++ {
			if i != 0 && nums[i] == nums[i-1] && !st[i-1] { // 去重，用st来判别是深度还是广度
				continue
			}
			if !st[i] {
				path = append(path, nums[i])
				st[i] = true
				dfs(nums, cur+1)
				st[i] = false
				path = path[:len(path)-1]
			}
		}
	}
	dfs(nums, 0)
	return rt
}
```

